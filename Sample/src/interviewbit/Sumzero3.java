package interviewbit;

import java.util.ArrayList;
import java.util.HashSet;
import java.util.LinkedHashMap;

public class Sumzero3 {
  public ArrayList<ArrayList<Integer>> threeSum(ArrayList<Integer> input) {
//    ArrayList<ArrayList<Integer>> retlist = new ArrayList<ArrayList<Integer>>();
//    HashSet<Integer> set = new HashSet<>();
//    for (int i = 0; i < input.size(); i++) {
//      set.add(input.get(i));
//    }
//    LinkedHashMap<HashSet<Integer>, Integer> Sset =
//        new LinkedHashMap<HashSet<Integer>, Integer>();
//    for (int i = 0; i < input.size(); i++) {
//      for (int j = 0; j < input.size(); j++) {
//        if (set.contains(-1 * (input.get(i) + input.get(j)))) {
//          HashSet<Integer> li = new HashSet<Integer>();
//          // HashSet
//          li.add(input.get(i));
//          li.add(input.get(j));
//          
//          li.add(-1 * (input.get(i) + input.get(j)));
//          // System.out.println();
//          if(!Sset.containsKey(li))
//          Sset.put(li, 0);
//        }
//      }
//    }
//    for (HashSet<Integer> li : Sset.keySet()) {
//      retlist.add(li);
//    }
//    return retlist;
    ArrayList<ArrayList<Integer>> retlist = new ArrayList<ArrayList<Integer>>();
    
    
    return retlist;
    
  }
  public static void main(String[] args) {
    
  }
}
